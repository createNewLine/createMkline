### 任务 1：启动速度 — 延迟渲染 SVG 图标
- 将 Icons::load() 从 Default::default() 中移除，改为首次 view 时惰性初始化（存 Option<Icons>，首次渲染时加载并缓存）。
- 或者将 SVG 预渲染结果序列化为二进制 .png 字节，编译期通过 include_bytes! 嵌入，运行时零开销加载 image::Handle::from_bytes（完全跳过 resvg / tiny-skia 运行时开销）。
- 此任务只涉及 svg.rs 和 main.rs 的 State/view 部分。
### 任务 2：启动速度 — 异步加载字体
- 将 std::fs::read 改为在 Application::new() 中通过 Command::perform 异步加载字体，窗口先用系统默认字体渲染，字体加载完成后通过消息更新字体。
- 如果方案太复杂就跑 spawn_blocking 读字体，窗口初始化不阻塞。
### 任务 3：迁移速度 — 多源目录并行处理
- 核心改动在 ops.rs 的 execute_confirm 和 main.rs 的 Message::Confirm。
- 方案：每个源目录独立跑一个 tokio::task::spawn_blocking，N 个源 = N 个并行任务，用 futures::join_all 或 tokio::join! 等待全部完成。
- 每个任务内部保留现有的串行逻辑（校验→复制→删除→建链），因为单条链路有顺序依赖。
需要考虑：多个源目录可能迁到同一个目标目录，文件名冲突检测需要先全部校验完再开始复制（两阶段：校验阶段并行，复制阶段并行）。
- 改动涉及 main.rs:193-224 和 ops.rs:117-179。
### 任务 4：迁移速度 — 单目录内并行文件复制
- 改动 copy_entry：对大目录递归时，用 rayon 或 tokio::spawn_blocking 并行复制多个子条目。
- 用 rayon::scope 递归进入子目录时 rayon::spawn 每个子任务。
也可考虑用 robocopy（Windows 内置，/MT[:N] 参数原生多线程复制）替代自定义的 copy_entry，省代码且更快。
- 改动主要在 ops.rs:6-28。
### 任务 5：备份 — 多源并行备份
- main.rs:258-278 的 BackupAll：将串行 iter().map() 改为并行 spawn_blocking，与任务 3 同理。
---
# 2026/05/20
修复这几个问题, 问题5这样修复: 如果源已被删除且创建软连接失败, 那么就判断源目录是否存在, 不存在则将迁移的目录复制回去,然后再清理目标目录. 问题6这样修复:  在用户浏览选择的时候加个判断是否有重复的文件夹名称或文件名称, 只判断名字, 不判断路径, 有重复就不填到输入框并且弹框提示[目录名]重复, 如果是用户手动填写则在点击确定按钮的时候进行一次统一校验, 有重复就不进行迁移并且弹框提示[目录名1]重复\n[目录名2]重复, 要修复的问题如下: 
1. futures 依赖是多余的 — 应该去掉
tokio 已经自带 join_all（在 full features 下）：tokio::try_join! 和 futures::future::join_all 功能完全等价。引入一个额外的 crate 只为了用一个宏，不符合"能简则简"原则。

建议： 将 futures::future::join_all 替换为 tokio::join! 或直接用 tokio::task::JoinSet，然后从 Cargo.toml 移除 futures。
5. 回滚逻辑的一致性
ops.rs:234-238：Phase 3 失败时回滚清理所有已复制目标。但如果某个 Phase 3 操作删除源成功但创建链接失败，回滚只清理了目标目录，没有恢复源（源已被删除）。
6. backup_validate + backup_copy 的竞态条件
Phase 1（validate）检查备份路径不存在，Phase 2（copy）开始复制。如果用户在同一父目录下有两个同名的源（来自不同路径 → 不同父目录），不会冲突。但如果备份验证和复制之间，外部进程创建了同名路径，会覆盖失败。这在原 backup_one 中也存在，所以不是回退。
---
# 2026/05/21
1.备份校验的重复名跟确定校验的不是同一个逻辑, 备份校验应该校验全路径, 确定校验目录名, 因为备份是在不同源目录下备份的, 而确定是要复制到同一个目标目录, 所以逻辑不一样, 照我说的把备份的重复校验逻辑改一下
2. 点确定迁移的时候不仅要校验源目录的输入框是否有重复名, 还应该校验目标目录下是否有跟源目录填写的是否有重复的文件名, 如果是目标目录下有跟源目录输入框有重复的文件名则提示: 目标目录已有[[目录名1], [目录名2]],是否继续迁移并覆盖. 这个提示要有确定和取消俩按钮, 确定则继续迁移并覆盖目标目录的文件夹, 取消则不进行迁移;
3. 只做以上优化改动, 原来的功能不要丢, 不要乱改